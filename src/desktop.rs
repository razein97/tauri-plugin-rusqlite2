use serde::de::DeserializeOwned;
use tauri::{plugin::PluginApi, AppHandle, Runtime};

use crate::models::*;

pub fn init<R: Runtime, C: DeserializeOwned>(
  app: &AppHandle<R>,
  _api: PluginApi<R, C>,
) -> crate::Result<Rusqlite<R>> {
  Ok(Rusqlite(app.clone()))
}

/// Access to the rusqlite APIs.
pub struct Rusqlite<R: Runtime>(AppHandle<R>);

impl<R: Runtime> Rusqlite<R> {
  pub fn ping(&self, payload: PingRequest) -> crate::Result<PingResponse> {
    Ok(PingResponse {
      value: payload.value,
    })
  }
}
